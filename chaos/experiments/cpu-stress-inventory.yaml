apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress-inventory
  namespace: bookstore
spec:
  mode: one
  selector:
    namespaces:
      - bookstore
    labelSelectors:
      app: inventory-service
  stressors:
    cpu:
      workers: 2
      load: 80
  duration: 2m
