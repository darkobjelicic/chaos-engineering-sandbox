apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: bookstore-dev

resources:
  - ../

patchesStrategicMerge:
  - replicas-patch.yaml

configMapGenerator:
  - name: env-config
    literals:
      - ENVIRONMENT=dev
      - LOG_LEVEL=DEBUG
