apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: bookstore-prod

resources:
  - ../

patchesStrategicMerge:
  - replicas-patch.yaml

configMapGenerator:
  - name: env-config
    literals:
      - ENVIRONMENT=prod
      - LOG_LEVEL=INFO
